<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AdministratorActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:title="@string/app_name"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

    </android.support.design.widget.AppBarLayout>

    <include
        android:id="@+id/content_administrator"
        layout="@layout/content_administrator" />

    <include
        android:id="@+id/content_last_connected_users"
        layout="@layout/content_last_connected_users"/>

    <include
        android:id="@+id/content_genres"
        layout="@layout/content_genres"/>

    <include
        android:id="@+id/content_last_searched_books"
        layout="@layout/content_last_searched_books"/>

    <include
        android:id="@+id/content_loggings_last_week"
        layout="@layout/content_loggings_last_week"/>

</android.support.design.widget.CoordinatorLayout>